# f8proclauncher

`serviceClass=f8.proclauncher`

Minimal service that launches an external OS process based on service state fields:

- `programPath: string`
- `singleton: bool` (default: true)
- `detached: bool` (default: true)

