schemaVersion: f8serviceEntry/1
serviceClass: f8.dl.detsorter
label: DL Detection Sorter
version: 0.0.1
launch:
  command: pixi
  args: ["run", "f8pydl_detsorter"]
  env: {}
  workdir: "../../../../"
